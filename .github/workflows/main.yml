name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world Mainak
    - name: Run a multi-line script
      run: |
        pwd
        ls -ltr
    - uses: actions/PublishBuildArtifacts@1
      name: Publish Artefacts
      inputs:
        PathtoPublish: "/"
      
